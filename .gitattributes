# Large media assets — stored via Git LFS instead of as regular git blobs.
# Run `git lfs install` once on your machine before committing these.
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
